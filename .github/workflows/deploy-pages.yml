# Her push'ta siteyi GitHub Pages'a yayınlar
name: GitHub Pages yayını

on:
  push:
    branches: [claude/mekatrohome-website-redesign-h3ff97]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Kodu al
        uses: actions/checkout@v4
      - name: Pages'ı yapılandır
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Site dosyalarını paketle
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Yayınla
        id: deployment
        uses: actions/deploy-pages@v4
